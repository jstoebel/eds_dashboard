= link_to student_pgp_goals_path(@student.id), class: "btn btn-default back-link glyphicon glyphicon-arrow-left" do
    %i.link-text Back

%h1 Editing PGP Goal
= simple_form_for @pgp_goal, :url => pgp_goal_path(@pgp_goal.id) do |f|
    %table
        = render partial: 'form', :locals => {f:f}
