<%= link_to student_pgps_path(@pgp.student.id), class: "btn btn-default back-link glyphicon glyphicon-arrow-left" do %>
  <i class=" link-text"> Back</i>
<% end %>
<%= form_for(@pgp, url: pgp_path(@pgp.id)) do |f| %>
<h2>Edit Professional Growth Plan for <%=@student.name_readable%></h2>
  <%= error_messages_for(@pgp) %>

    <table summary="Edit PGP form fields">

	<%= render(partial: 'form', locals: {f: f}) %>

	<div class="form-buttons">
    	<tr>
    	    <td> <%= submit_tag("Update Professonal Growth Plan") %> </td>
        </tr>
	</div>

<% end %>
