<%= link_to student_issues_path(@student.AltID), class: "btn btn-default back-link glyphicon glyphicon-arrow-left" do %>
  <i class=" link-text"> Back</i>
<% end %> 

<table class="table table-striped">

  <caption>
    <div class="header1">
      <%="#{@student.FirstName} #{@student.LastName}"%>
    </div>

    <div class="header2">Updates for issue: <%= @issue.Name %></div>
  </caption>

  <% if @updates %>
    <tr>
      <th>Update</th>
      <th>Date</th>
    </tr>
  

    <% @updates.each do |update| %>

      <tr>
        <td><%=update.UpdateName%></td>
        <td> <%=update.CreateDate.strftime("%m/%d/%Y")%> </td>
        <td class="actions">
          <%= link_to "Details", issue_update_path(update.UpdateID), class: "btn btn-info action-small"%>
        </td>
      </tr>
      
    <% end %>

  <% end %>

</table>