= link_to pgp_goal_pgp_scores_path(@pgp_goal), class: "btn btn-default back-link glyphicon glyphicon-arrow-left" do
  %i.link-text Back

%h1= "New PGP Score for #{@pgp_goal.name}"
%h2= @pgp_goal.student.name_readable
= form_tag pgp_goal_pgp_scores_path(@pgp_goal) do
  = render partial: 'form', :locals => {items: @assessment_items}