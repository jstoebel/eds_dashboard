<!-- TODO back button -->

<%= form_for(@site, url: clinical_sites_path) do |f| %>

	<table summary="clinical_site form fields">

    <caption>
    	<span class="header1"> Create New Clinical Site</span>

    </caption>

	<%= render(partial: 'form', locals: {f: f}) %>

	<tr>
		<td><%= submit_tag("Update Site") %></td>
	</tr>

<% end %>