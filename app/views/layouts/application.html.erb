<!DOCTYPE html>
<html>
	<head>
	  <title>Education Studies Dashboard</title>

    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>

    <script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css"/>

    <%= stylesheet_link_tag('application') %>
    <%# javascript_include_tag :defaults %>

    <%= javascript_include_tag params[:controller] %>
    <%= javascript_include_tag 'application' %>
	</head>
	<body>
<!-- NEW HEADER STARTS HERE -->
<div class="navbar navbar-default">
  <div class="container-fluid">

    <!-- menu to swap psudo role if user is admin -->
    <% if session[:role] == 'admin' %>

      <p class="navbar-text pull-left">
      <%= select_tag "view_as", 
          options_for_select(Role.all.order(:RoleName).collect { |r| [r.RoleName, r.idRoles]}),
            {:prompt => "View Site As...",
            :class => "form-control"}
         %>
      </p>

    <% end %>

    <p class="navbar-text pull-right">
       
            Welcome, <%= "#{session[:user]} (#{session[:role]})" %>
              
            <%= link_to "/logout", :class => "btn btn-default" do %>
              Log Out
            <% end %>
             
    </p>  

  </div>
      <!-- TODO Here is where we render role specific nav bar -->
  <div class="container-fluid">
    <div class="navbar-header">
      <span class="navbar-brand">Educaiton Studies Dashboard</span>
    </div>
    <ul class="nav navbar-nav">
      <li class="active"><%= link_to("Advisees", students_path) %></li>
      <li class="active"><%= link_to("Praxis Registration", new_praxis_result_path) %></li>
      <li class = "dropdown active">        
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> Admissions/Exit <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><%= link_to("Admission to TEP", adm_tep_index_path) %></li>
          <li><%= link_to("Admission to Student Teaching", adm_st_index_path) %></li>
          <li><%= link_to("Program Exits", prog_exits_path) %></li>
        </ul>
      </li>

      <li class="dropdown active">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Field Experience <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><%= link_to("Clinical Placements", clinical_assignments_path) %></li>
          <li><%= link_to("Clinical Teachers", 
          clinical_teachers_path) %></li>
          <li><%= link_to("Clinical Sites", 
          clinical_sites_path) %></li>
        </ul>
      </li>
    </ul>

  </div>

</div>

		<!--Before yield -->
    <%= params %>
      <% if !flash[:notice].blank? %>
        <div class="notice">
          <%= flash[:notice] %>
        </div>
      <% end %>
<div class="yield">  <%= yield %> </div>
	
		<!--After yield -->

</html>
