 
    <%= error_messages_for(@assessment) %>
    <tr>
      <th> <%= f.label(:Name, "Name")%> </th>
      <td> <%= f.text_field(:Name) %></td>
    </tr>
    
    <tr>
      <th> <%= f.label(:Description, "Subject")%> </th>
      <td> <%= f.select(:Subject, @subjects.map {|s| [s.EDSProgName, s.EDSProgName]},
        {:prompt => 'Select a subject'}) %></td>
    </tr>

    <tr>
      <th> <%= f.label(:Rank, "Rank")%> </th>
      <td> <%= f.text_field(:Rank) %></td>
    </tr>

    <tr>
      <th> <%= f.label(:YearsExp, "Years Experience")%> </th>
      <td> <%= f.text_field(:YearsExp) %></td>
    </tr>

  </table>
 