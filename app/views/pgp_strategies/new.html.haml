= link_to pgp_goal_pgp_strategies_path(@pgp_goal.id), class: "btn btn-default back-link glyphicon glyphicon-arrow-left" do
    %i.link-text Back

%h1 New PGP Strategy
= simple_form_for @pgp_strategy, :url => pgp_strategies_path do |f|
    %table
        = render partial: 'form', :locals => {f:f}