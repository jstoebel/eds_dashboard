public/student_files/*
public/assets/*
/tmp/cache/*
/log/*
*.rbc
capybara-*.html
.rspec
/log
/tmp
/db/*.sqlite3
/db/*.sqlite3-journal
/public/system
/coverage/
/spec/tmp
**.orig
rerun.txt
pickle-email-*.html
.DS_Store
passenger.*.pid.lock

#seed data that can't go into the repo
/db/seed/secure_seed/*

shared

#not checking in database.yaml since different deployments need to configure it differently (c9.io users)
config/database.yml

# TODO Comment out these rules if you are OK with secrets being uploaded to the repo
config/initializers/secret_token.rb
config/secrets.yml

## Environment normalisation:
/.bundle
/vendor/bundle

# these should all be checked in to normalise the environment:
# Gemfile.lock, .ruby-version, .ruby-gemset

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc

# if using bower-rails ignore default bower_components path bower.json files
/vendor/assets/bower_components
*.bowerrc
bower.json

# Ignore pow environment settings
.powenv
error_rpt.txt
error_report.txt
entrance_exit_FIXED.csv

# only .rb and .rake in lib/tasks/reports
lib/tasks/reports/**
!lib/tasks/reports/**/*.rb
!lib/tasks/reports/**/*.rake

config/chef/nodes/*
